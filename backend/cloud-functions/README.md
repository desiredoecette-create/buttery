# Cloud Functions

Reserved for trusted operations such as username reservation, share notifications, media cleanup,
and future import processing. Prefer callable/HTTPS functions only when direct Firestore access
cannot safely express the operation.

Bootstrap TypeScript functions here when the first server-side workflow is implemented.
