# Feature module targets

Introduce modules incrementally: `auth`, `recipes`, `albums`, `favorites`, `grocery`,
`sharing`, `inbox`, `profile`, `continue-recipe`, `import`, `cooking`, and `ambient`.
Each owns its screens, state holder/ViewModel, and feature navigation entry point.
